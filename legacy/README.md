# ClientStorage

`ClientStorage` package is deprecated.

Use the scoped package instead:

```sh
npm install @veliovgroup/client-storage
```

Legacy install command still works through explicit alias for now:

```sh
npm install ClientStorage@npm:@veliovgroup/client-storage
```

Runtime behavior is a thin re-export with one-time migration warning.
